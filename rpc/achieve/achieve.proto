syntax = "proto3";

package achieve;
option go_package="./achieve";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Achieve {
  rpc Ping(Request) returns(Response);
}
